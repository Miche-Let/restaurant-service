spring:
  application:
    name: restaurant-service
  data:
    web:
      pageable:
        default-page-size: 10
        max-page-size: 50

server:
  port: 19300

management:
  endpoints:
    web:
      exposure:
        include: health,info